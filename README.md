# Compile-R
React 19 RC Upgrade. New experimental `React Compiler` .  New features experiment: Using &lt;form> Actions. New hook: `useActionState` . React DOM: New hook: `useFormStatus` . New API: `use`.  `ref` as a prop.
